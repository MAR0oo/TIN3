<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Błąd walidacji</title>
</head>
<body>
<h1><%= message %></h1>

<% if (errors && errors.length > 0) { %>
    <ul>
        <% for (let error of errors) { %>
            <li><%= error %></li>
        <% } %>
    </ul>
<% } else { %>
    <p>Brak szczegółowych błędów do wyświetlenia.</p>
<% } %>

<a href="/form">Wróć do formularza</a>
</body>
</html>
